version: '2'

services:
  db:
    image: postgres
    ports:
      - "5432:5432"
  web:
    build: .
    command: uwsgi --http :8000 --wsgi-file app/wsgi.py 
    volumes:
      - .:/code
    ports:
      - "8000:8000"
    depends_on:
      - db
