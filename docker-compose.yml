version: '2'

services:
  db:
    image: postgres
    ports:
      - "5432:5432"
  web:
    build: .
    command: uwsgi --http :8000 --module app.wsgi 
    volumes:
      - .:/code
    ports:
      - "8000:8000"
    depends_on:
      - db
